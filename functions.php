<?php
function getTasks($user_id) {
    // Fetch tasks from the database for a given user
    // ...

    return $tasks;
}

function createTask($user_id, $task_data) {
    // Create a new task in the database
    // ...

    return $task_id;
}

// Other utility functions
